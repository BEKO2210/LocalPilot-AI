import { buildStubProvider } from "./_stub";

/**
 * Gemini-Provider — Stub. Wird in einem späteren Meilenstein scharf
 * gemacht (Google `@google/generative-ai`-SDK), wenn Bedarf besteht.
 */
export const geminiProvider = buildStubProvider(
  "gemini",
  "Gemini-Provider ist noch nicht implementiert. Folgt in einer späteren Code-Session.",
);
